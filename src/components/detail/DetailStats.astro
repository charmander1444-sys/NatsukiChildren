---
interface Props {
  stats?: Record<string, string>;
}

const { stats } = Astro.props;

const LABELS: Record<string, string> = {
  fuerza: "Fuerza",
  resistencia: "Resistencia",
  velocidad: "Velocidad",
  inteligencia: "Inteligencia",
  habilidades_combate: "Habilidades de combate",
  carisma: "Carisma",
  portal: "Puerta",
  afinidad_espiritual: "Afinidad espiritual",
  potencial: "Potencial",
  suerte: "Suerte",
};

const RANKS = ["F", "E", "D", "C", "B", "A", "S", "SS", "SS+", "EX"];

const RANK_COLORS: Record<string, string> = {
  F: "#52525b",
  E: "#71717a",
  D: "#8b8398",
  C: "#8b93b0",
  B: "#60a5fa",
  A: "#818cf8",
  S: "#b48cff",
  SS: "#e879f9",
  "SS+": "#f472b6",
  EX: "#facc15",
};

const RANK_GLOW: Record<string, string> = {
  F: "none",
  E: "none",
  D: "none",
  C: "0 0 4px #8b93b055",
  B: "0 0 6px #60a5fa66",
  A: "0 0 8px #818cf877",
  S: "0 0 10px #b48cff88",
  SS: "0 0 12px #e879f999",
  "SS+": "0 0 14px #f472b6aa",
  EX: "0 0 18px #facc15cc",
};

const RANK_DESC: Record<string, string> = {
  F: "Ninguna",
  E: "Mala",
  D: "Débil",
  C: "Media",
  B: "Buena / Superior al promedio",
  A: "Excelente",
  S: "Superior",
  SS: "Élite",
  "SS+": "Divina",
  EX: "Incalculable",
};

const entries = stats
  ? Object.entries(stats).filter(([, v]) => v && RANKS.includes(v.toUpperCase()))
  : [];
---

{entries.length > 0 && (
  <div class="@container rounded-2xl border border-rz-accent/20 bg-rz-bg-2/40 p-6">
    <h2 class="font-display text-xl text-white text-glow mb-5 text-center">Stats</h2>
    <div class="space-y-4">
      {entries.map(([key, rawRank]) => {
        const rank = rawRank.toUpperCase();
        const idx = RANKS.indexOf(rank);
        const pct = ((idx + 1) / RANKS.length) * 100;
        const color = RANK_COLORS[rank];
        const glow = RANK_GLOW[rank];
        return (
          <div class="flex items-center gap-3 @max-[400px]:flex-wrap @max-[400px]:gap-y-1.5" title={RANK_DESC[rank]}>
            <span class="w-40 shrink-0 text-sm uppercase tracking-wider text-rz-text/70 font-body @max-[400px]:w-auto @max-[400px]:flex-1 @max-[400px]:order-1">
              {LABELS[key] ?? key}
            </span>

            <div class="flex-1 h-3 rounded-full bg-rz-bg/70 overflow-hidden @max-[400px]:basis-full @max-[400px]:order-3">
              <div
                class="h-full rounded-full transition-all duration-500"
                style={`width:${pct}%; background:${color}; box-shadow:${glow};`}
              ></div>
            </div>

            <span
              class="w-14 shrink-0 text-center text-sm font-display font-semibold rounded-full py-0.5 @max-[400px]:w-auto @max-[400px]:px-2 @max-[400px]:order-2"
              style={`color:${color}; border:1px solid ${color}55; background:${color}14;`}
            >
              {rank}
            </span>
          </div>
        );
      })}
    </div>
  </div>
)}